package com.viettel.etc.repositories.tables;import com.viettel.etc.repositories.tables.entities.CatsPhasesEntity;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.data.jpa.repository.Query;import org.springframework.data.repository.query.Param;import org.springframework.stereotype.Repository;import java.sql.Date;import java.util.List;/** * Autogen class Repository Interface: Create Repository For Table Name Cats_phases * * @author ToolGen * @date Mon Sep 14 15:27:31 ICT 2020 */@Repositorypublic interface CatsPhasesRepositoryJPA extends JpaRepository<CatsPhasesEntity, Long> {    @Query("SELECT cp FROM CatsPhasesEntity cp WHERE cp.phaseId = :phaseId AND cp.fromDate < :nowDate AND cp.toDate > :nowDate AND cp.isActive = 1 AND cp.isDelete = 0")    List<CatsPhasesEntity> findByPhaseIdAndFromDateIsLessThanAndToDateIsGreaterThan(@Param("phaseId") Integer phaseId, @Param("nowDate") Date nowDate);}